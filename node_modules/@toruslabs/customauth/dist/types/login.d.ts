import NodeDetailManager from "@toruslabs/fetch-node-details";
import Torus from "@toruslabs/torus.js";
import { AggregateLoginParams, CustomAuthArgs, ExtraParams, HybridAggregateLoginParams, InitParams, RedirectResult, RedirectResultParams, SingleLoginParams, TorusAggregateLoginResponse, TorusHybridAggregateLoginResponse, TorusKey, TorusLoginResponse, TorusSubVerifierInfo } from "./handlers/interfaces";
import SentryHandler from "./sentry";
import { UX_MODE_TYPE } from "./utils/enums";
import StorageHelper from "./utils/StorageHelper";
declare class CustomAuth {
    isInitialized: boolean;
    config: {
        baseUrl: string;
        redirectToOpener: boolean;
        redirect_uri: string;
        uxMode: UX_MODE_TYPE;
        locationReplaceOnRedirect: boolean;
        popupFeatures: string;
    };
    torus: Torus;
    nodeDetailManager: NodeDetailManager;
    storageHelper: StorageHelper;
    sentryHandler: SentryHandler;
    constructor({ baseUrl, network, enableLogging, enableOneKey, redirectToOpener, redirectPathName, apiKey, uxMode, locationReplaceOnRedirect, popupFeatures, metadataUrl, storageServerUrl, networkUrl, sentry, }: CustomAuthArgs);
    init({ skipSw, skipInit, skipPrefetch }?: InitParams): Promise<void>;
    triggerLogin(args: SingleLoginParams): Promise<TorusLoginResponse>;
    triggerAggregateLogin(args: AggregateLoginParams): Promise<TorusAggregateLoginResponse>;
    triggerHybridAggregateLogin(args: HybridAggregateLoginParams): Promise<TorusHybridAggregateLoginResponse>;
    getTorusKey(verifier: string, verifierId: string, verifierParams: {
        verifier_id: string;
    }, idToken: string, additionalParams?: ExtraParams): Promise<TorusKey>;
    getAggregateTorusKey(verifier: string, verifierId: string, // unique identifier for user e.g. sub on jwt
    subVerifierInfoArray: TorusSubVerifierInfo[]): Promise<TorusKey>;
    getPostboxKeyFrom1OutOf1(privKey: string, nonce: string): string;
    getRedirectResult({ replaceUrl, clearLoginDetails }?: RedirectResultParams): Promise<RedirectResult>;
    private handlePrefetchRedirectUri;
}
export default CustomAuth;
